{
    "name": "mehrdad-app-backend",
    "version": "1.0.0",
    "description": "Full Stack Persian Transaction Tracker",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "npm run build --prefix frontend && wrangler deploy",
        "build": "npm run build --prefix frontend"
    },
    "dependencies": {
        "hono": "^4.2.4"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240405.0",
        "typescript": "^5.4.3",
        "wrangler": "^3.45.0"
    }
}